# OrbitLens-AI
Satellite/mission telemetry dashboard that detects anomalies (statistical + isolation forest) and generates plain-English insights — no LLM or external API required. React + Vite frontend, FastAPI backend. Upload CSV telemetry, view health score and anomaly charts, generate insights, export a Markdown/PDF mission report.
